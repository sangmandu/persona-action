{
  "name": "persona-action",
  "version": "0.1.0",
  "description": "Generate personalized Claude Code sub-agents from contributors' merged PR history",
  "type": "module",
  "bin": {
    "persona-action": "./src/cli.ts"
  },
  "scripts": {
    "run": "bun run src/cli.ts",
    "validate": "bun run src/cli.ts validate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "yaml": "^2.6.0",
    "micromatch": "^4.0.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/micromatch": "^4.0.9",
    "typescript": "^5.6.0"
  }
}
